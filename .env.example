# Client-safe Vite envs (VITE_* are exposed to the client)
VITE_ANILIST_URL=https://graphql.anilist.co

# Enable debug logging for concurrency limiter and retries ("1" to enable)
VITE_ANILIST_DEBUG=0

# Optional timeout in milliseconds for AniList GraphQL requests.
# Example: 10000 for 10 seconds. Leave blank or remove to disable.
VITE_ANILIST_TIMEOUT_MS=10000

# Optional: future use for authenticated/user-specific queries
VITE_ANILIST_TOKEN=
